apiVersion: v1
kind: Service
metadata:
  name: {{ include "elementa.fullname" . }}
  labels:
    {{- include "elementa.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "elementa.selectorLabels" . | nindent 4 }}
